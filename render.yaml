services:
  - type: web
    name: grid-wallet
    runtime: static
    buildCommand: echo "bundle is prebuilt and committed"
    staticPublishPath: ./public
    pullRequestPreviewsEnabled: false
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
